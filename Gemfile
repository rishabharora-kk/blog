# Only needed for optional local preview:
#   bundle install && bundle exec jekyll serve
# GitHub Pages builds this site without it.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick"
